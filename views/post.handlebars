<div class="text-center">
  <h2>{{title}}</h2>
  <p><i>Created by {{user.first_name}} on {{format_date date_created}}</i></p>
  <p>{{content}}</p>
</div>
{{#if comments.length}}
{{#each comments as |comment|}}
<div class="text-center">
  <p><strong>{{format_date comment.date_created}} - {{comment.user.first_name}}:</strong> {{comment.content}}</p>
</div>
{{/each}}
{{/if}}